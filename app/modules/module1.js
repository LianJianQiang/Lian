// actionType

// action
export const action1 = () => {
    return {};
};

// reducer
export const modelList = (state = {
    expire: true,
    modelList: []
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

